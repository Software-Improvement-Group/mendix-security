rules:
- id: runtime-settings-multiplesessions
  patterns:
    - pattern-inside: |
        "settingsParts": [...]
    - pattern: |
        "allowUserMultipleSessions": true
  message: Multiple sessions per user should be disabled in Runtime Settings to restrict authentication attempts from different locations
  languages: [json]
  severity: WARNING
  metadata:
    cwe: cwe-307
